#programa17
kilos=input("introduce el valor de tu peso")
altura=input("introduce el valor de tu altura")
imc=float(kilos)/float(float(altura)**2)
peso=(" ")
if (imc) >25 or (imc)==25
pesos=("hay sobrepeso")
print(f"si pesas {kilos} kilos y mides {altura}, tu IMC es :{inc:.2f} . ",peso)

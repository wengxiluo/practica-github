#programa15
import math
radio=input("introduce el valor de radio de un cllindre")
altura=float(radio)*float(radio)*math.pi*2+float(altura)*float(radio)*math.pi
volumen=float(radio)*float(radio)*math.pi*float(altura)
print(f"El área del cllindro és:{area:.2f}")
print(f"El volumen del cllindro és:{volumen:.2f}")
